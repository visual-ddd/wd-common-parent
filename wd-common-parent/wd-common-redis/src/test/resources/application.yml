spring:
  main:
    allow-bean-definition-overriding: true
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  redis:
    host: ${redis.host}
    password: ${redis.password}
    port: ${redis.port}

redis:
  host: 127.0.0.1
  port: 6379
  password: 123456
#  prefix: dss
  suffix: ''

common:
  redis-lock:
    enable: true
