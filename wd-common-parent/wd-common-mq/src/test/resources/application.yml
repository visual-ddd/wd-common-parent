server:
  port: 8081

common:
  mq:
    message:
      default-type: rabbitMq
    rabbitmq:
      auto-create-topic: true
      bootstrap-servers: host
      username: username
      password: password
      queue-name: 'queue-id-test'